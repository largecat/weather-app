const input = document.querySelector('.search');
const searchBtn = document.querySelector('.search-btn');
const weatherDisplayArea = document.getElementById('weather-display');
const displayLocation = document.getElementById('location');
const displayWeatherTemp = document.getElementById('temp');
const displayWeatherDesc = document.getElementById('description');
const infoDesc = document.getElementById('info');
const advInfo = document.getElementById('adv-info');
const sevenDayDisplay = document.querySelector('.seven-day');
const leftSide = document.querySelector('.left-side');
const displayHumidity = document.getElementById('humidity');
const displayRainChance = document.getElementById('rain-chance');
const displayWind = document.getElementById('wind');

export {
  input,
  searchBtn,
  weatherDisplayArea,
  displayLocation,
  displayWeatherTemp,
  displayWeatherDesc,
  infoDesc,
  advInfo,
  sevenDayDisplay,
  leftSide,
  displayHumidity,
  displayRainChance,
  displayWind,
};
